import { Paper } from "@material-ui/core";
import React from "react";
import styles from "./style.module.scss";

const WeatherInfo = () => {
    return (
        <div>
            <Paper className={styles.container}></Paper>
        </div>
    );
};

export default WeatherInfo;
